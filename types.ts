
// Define um enum para as páginas do site.
// Isso ajuda a evitar erros de digitação e mantém o código de navegação consistente.
export enum Page {
  Home,
  About,
  Projects,
  Volunteer,
  Contact,
}
